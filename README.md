# monitor-de-temperatura
 monitora temperatura da CPU e GPU,
arquivo executável esta na pasta dist, para funcionar é preciso ter o comando sensors instalado nessa primeira versão, então para isso você precisa instalar o lm_sensors no linux para funcionar 
